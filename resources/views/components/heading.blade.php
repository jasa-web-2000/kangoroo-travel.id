<div class="{{ $full ? 'mr-auto lg:mx-auto' : 'mx-auto' }} max-w-2xl lg:text-center">

    <h2 class="text-base font-semibold leading-7 {{ $light ? 'text-yellow-500' : 'text-blue-600' }}">{{ $title }}
    </h2>
    <p class="mt-2 text-3xl font-bold tracking-tight {{ $light ? 'text-blue-50' : 'text-gray-900' }} sm:text-4xl">
        {{ $desc1 }}</p>
    <p class="mt-6 {{ $light ? 'text-blue-200' : 'text-gray-600' }}">{{ $desc2 }}</p>
</div>
